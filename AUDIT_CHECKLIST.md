# AUDIT CHECKLIST — Portfolio fixes

> Action list generated from the 2026-06-05 content + UX audit. Source critique lives in chat; this file is the working checklist.
>
> ## CURRENT SCOPE: CONTENT ONLY
>
> Focus right now: **copy quality, factual accuracy, metadata, naming/wording, case-study narratives, content gaps, missing assets.** No layout changes, no type hierarchy changes, no animation work, no component restructuring.
>
> Anything that touches CSS positioning, animation, sticky behavior, ambient effects, sectioning, or visual hierarchy → parked in the **Deferred — design pass** section at the bottom.
>
> **Conventions:** `- [ ]` = open · `- [x]` = done · `- [~]` = in progress · `- [-]` = decided to skip · `- [⏸]` = deferred to design pass
>
> **Coordinate with the other session before editing shared files.**

---

## ⏸️ Current state — last touched 2026-06-08 (Mon)

### What's landed and verified in the browser
- **Phase 0** — all 13 content decisions made. 5 design decisions parked for later.
- **Phase 1** — critical content/bug fixes (contact form stripped, Pro5 link wired, footer year dynamic, phone removed, lab pages noindex, Resume page TODO cleaned). Carryover: OG PNG.
- **Phase 2** — naming + copy sweep complete. Spec Sheets / Datasheet / Catalog / Vol. I-III vocabulary dropped everywhere except `Plate XX` on case studies (per 0.1). Title tags include role on every page. Story pull-quote rewritten with derived counts. Numbers stats rebalanced.
- **Round 1 + 2 link research** — 14 of 19 case studies enriched with real store metadata. Verified count: 11 → 13.
- **Phase 3 narrative polish** _(2026-06-08)_ — 3 thin case studies (careshare, synergy, warden-tracker) expanded with substantive prose. Other 16 already substantive or intentionally thin.
- **Per-MDX TODO cleanup** _(2026-06-08)_ — 9 of 19 files had resolved `todos:` pruned; 10 retain genuine open items.
- **Phase 4 SEO/metadata** _(2026-06-08)_ — `/resume` and `/contact` descriptions polished from generic placeholders to role-aware sentences. Status-badge legend added under stats grid on `/work`. Sitemap `/lab/*` exclusion verified. JSON-LD already at `MobileApplication` tier (better than original spec).

### What's blocked on the user
1. **Play Store install bands + ratings** for the 9 verified Play Store apps — see `PROJECT_LINKS.md` template. Once provided, I wire them into each Results section in a single batched pass.
2. **1200×630 OG PNG** at `public/og-default.png` — I can't generate PNG. Once dropped, I swap the reference in `Base.astro`.
3. ~~**`site` URL confirmation**~~ — ✅ confirmed 2026-06-08: `https://mitulvaghasiya.com` is the working URL for now.
4. **`/uses` page data** (optional Phase 6) — your setup: editor, shell, hardware (laptop / monitor / phone / keyboard), daily tools. Without this, the page can't be built with real content.
5. **(Optional)** Havitglam KR iOS "Largo 라르고" URL.
6. **(Optional)** Tier 2/3 metrics for any project (crash-free %, retention, customer count).

### What's parked for later
- **Phase 6 additions** beyond `/uses` — talks/press section (only if applicable), blog or `/notes` (deferred design styling).
- **Design pass** — hero hierarchy (0.2), Picks layout (0.3), ambient effects (0.4), section order (0.13), headshot placement (0.12), animation audit. Kicks in once content is locked.

### How to resume
- Read the `Decisions log` section (bottom) for the most recent context.
- Tell me which blocked item you have data for, or just say "continue" and I'll work on whatever isn't blocked.

---

---

## Workflow

1. Answer **Phase 0** content decisions inline (one question at a time via chat).
2. Land **Phase 1** (risk-free content/bug fixes) in parallel.
3. Phases 2–6 roll in order. Each phase is a checkpoint — land, review, move on.
4. Design pass is a separate future phase — do not mix.

---

## Phase 0 — Content decisions (BLOCKING)

Answers logged as they come in. Each decision unlocks specific tasks in Phase 2+.

| #    | Decision                                                                                                                                        | Choice                                                                                                                                                                                                                                                  |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0.1  | Specimen / Datasheet / Catalog vocabulary — `drop entirely` / `keep "Plate 01" on case studies only` / `keep all`                               | **Keep "Plate 01" on case studies only** ✅                                                                                                                                                                                                             |
| 0.5  | Pro5 cert — `provide URL: __________` / `drop until I have it`                                                                                  | **URL: https://drive.google.com/file/d/1O7zn6UVjArLrAM5f_UywToehhe3oCe3-/view?usp=sharing** ✅                                                                                                                                                          |
| 0.6  | Phone number — `publish on /contact` / `gate behind form only` / `remove from public source`                                                    | **Remove from profile.ts entirely (hide everywhere)** ✅                                                                                                                                                                                                |
| 0.7  | Contact form backend — `Formspree, ID: __________` / `strip to mailto + LinkedIn` / `Resend serverless`                                         | **Strip the form — mailto + LinkedIn / GitHub / X buttons only** ✅                                                                                                                                                                                     |
| 0.8  | Story pull-quote — `keep generic` / `rewrite specific (Diamonds / Toronto / Tokyo)` / `custom direction: __________`                            | **Numbers-led, Draft A: "Six years, _nineteen apps shipped_, _eleven still live_ in the stores."** ✅                                                                                                                                                   |
| 0.9  | PostgreSQL label in skills — `Strong` / `Learning` (pick one, not both)                                                                         | **Strong — drop from currentlyLearning** ✅                                                                                                                                                                                                             |
| 0.10 | `/lab/*` pages — `noindex` / `move out of src/pages/` / `delete`                                                                                | **Add `noindex` — keep URLs accessible to you, hide from Google** ✅                                                                                                                                                                                    |
| 0.11 | Real case-study metrics — `I have numbers for: __________` / `none, write all as "metrics private, distribution verified"`                      | **DEFERRED — user will gather Tier 1 (store stats) + Tier 2/3 (where remembered) per-project numbers and reply in a follow-up. Then we redesign Results sections together.** ⏸                                                                          |
| 0.12 | Headshot — `use inputs/old-portfolio/MR0100/assets/mitul.jpg now` / `wait for fresh photo` / `skip headshot entirely`                           | **Image available in inputs/ — display placement deferred (sizing, page, where) is a design discussion** ⏸                                                                                                                                              |
| 0.14 | Client/project name anonymity — confirm which can be public: `Synergy` / `Setera (TPMS partner)` / `Warden Tracker client` / `CareShare client` | **Synergy: use name publicly · Setera: anonymize → "automotive hardware partner, TPMS sensor utility" · Warden Tracker: anonymize → "geo-fencing system, B2B client" · CareShare: anonymize → "consumer health-care client project"** ✅                |
| 0.15 | KGK Diamonds headline metric — `50,000+ certified diamonds` / `120 years` / `neither` / `other: __________`                                     | **Use both — "120-year diamond house, 50,000+ certified inventory"** ✅                                                                                                                                                                                 |
| 0.16 | Today's Quote — confirm the in-app "2D-Game" mechanic (best guess: leaderboard mini-game)                                                       | **Leaderboard mini-game tied to quote engagement** ✅                                                                                                                                                                                                   |
| 0.17 | "Currently learning" list accuracy — currently `Rust / Server-side Dart / PostgreSQL`. Edit?                                                    | **Keep `Rust / Server-side Dart` after PostgreSQL removal** ✅                                                                                                                                                                                          |
| 0.18 | Voice/tone for case-study Results rewrites — `precise technical` / `narrative storytelling` / `bullet-only`                                     | **Keep current — precise technical prose** ✅                                                                                                                                                                                                           |
| 0.19 | Sadguru Soft promotion dates (Jr → Flutter → Expert → Senior) — provide or skip                                                                 | **No exact dates. Foreground the progression as a strong sentence right under the role title (not buried in highlights). Copy: "Promoted four times in 5+ years: Jr Flutter Dev → Flutter Dev → Expert Flutter Dev → Senior Mobile App Developer."** ✅ |
| 0.20 | Resume `bioLong[0]` says "five years at Sadguru Soft", site headline says "6+ years" — reconcile how?                                           | **Keep "6+" headline. Bump to "7+" once 7-year mark hits. Tighten `bioLong[0]` "five years" → "5+ years" for precision (actual: 5y 4m at Sadguru, 1y 3m at KGK = 6y 7m total).** ✅                                                                     |

---

## Phase 1 — Critical content/bug fixes (no decisions, low risk)

- [x] Contact form stripped — replaced with mailto + LinkedIn + GitHub + Twitter/X button row (per 0.7). Panel header changed to "Reach out", verified in browser. → [src/pages/contact.astro](src/pages/contact.astro)
- [x] TODO banner on `/contact` removed (form gone). → [src/pages/contact.astro](src/pages/contact.astro)
- [x] Footer year dynamic — `new Date().getFullYear()`, renders "© 2026" today. → [src/components/Footer.astro:4](src/components/Footer.astro)
- [x] Resume page "TODO: Pro5 verification link" rendering removed. Pro5 link now wired and clickable (Drive URL). → [src/pages/resume.astro](src/pages/resume.astro)
- [x] Phone removed from `profile.ts` (no references in codebase, safe drop). → [src/data/profile.ts](src/data/profile.ts)
- [x] Lab pages noindex — both `/lab/bg-variants` and `/lab/project-variants` already have `<meta name="robots" content="noindex" />` in their head. Sitemap (`sitemap.xml.ts`) explicitly enumerates only `/`, `/work`, `/resume`, `/contact` + work slugs — lab pages excluded by construction.

**Phase 0 cascade applied in same pass:**
- [x] PostgreSQL dropped from `currentlyLearning` (0.9) — home Now console now shows "Rust / Server-side Dart"
- [x] `bioLong[0]` tightened "five years" → "5+ years" (0.20)
- [x] Experience highlights[0] foregrounded — "Promoted four times in 5+ years: Jr Flutter Dev → Flutter Dev → Expert Flutter Dev → Senior Mobile App Developer." (0.19)
- [x] Pro5 cert URL wired into `certifications[0].url` (0.5)

**Phase 1 carryover (needs you, not me):**
- [ ] Replace `og-default.svg` with 1200×630 OG PNG — I can't generate PNG. Drop a generated image at `public/og-default.png` and I'll swap the reference in `Base.astro:21`.

---

## Phase 2 — Naming + copy sweep (content only)

Wording changes only. No type sizing, no hierarchy, no layout work.

### Nav + page chrome (text labels only)

- [x] Nav labels: `00 — Index / 01 — Catalog / 02 — Datasheet / 03 — Contact` → `Home / Work / Resume / Contact` → [src/components/Nav.astro](src/components/Nav.astro)
- [x] Nav tagline `Spec. Sheets Vol. I` removed → [src/components/Nav.astro](src/components/Nav.astro)
- [x] `/work` H1 `Catalog.` → `Work.` → [src/pages/work/index.astro](src/pages/work/index.astro)
- [x] `/work` ribbon → `Work / N projects` → [src/pages/work/index.astro](src/pages/work/index.astro)
- [x] `/work` Base `title=` prop → `Work`, description updated
- [x] `/work` SpecimenHeading titles → `Atlas` (was `Plate · atlas of work`) and `All projects` (was `Catalog · specimen index`)
- [x] `/work` sort label `Order` → `Featured`
- [x] `/resume` kicker `Curriculum vitae` → `Resume` (H1 stays as the name; the kicker now carries the "Resume" word for SEO/searchability)
- [x] `/resume` ribbon → `Resume · 2026.06` → [src/pages/resume.astro](src/pages/resume.astro)
- [x] `/resume` Base `title=` prop → `Resume`, description updated
- [x] `/resume` section headings → `Experience` / `Skills` / `Education` / `Contact` (was `travel log / atlas key / Schooling / channels`)
- [x] `/resume` "Specimens shipped here" → "Projects shipped here"
- [x] `/contact` ribbon → drop `Vol. III`, replace right-side `Initiate transmission` → `Email is fastest`
- [x] Case-study ribbon `Specimen Sheets · Vol. I · Plate XX` → keep `Plate XX` only; `← Catalog` → `← Work` → [src/layouts/CaseStudy.astro](src/layouts/CaseStudy.astro)
- [x] Case-study right-rail `Specimen` label → `Plate` (matches the ribbon)
- [x] Footer subtitle `Specimen Sheets · Atlas of Work · Vol. I` removed → [src/components/Footer.astro](src/components/Footer.astro)
- [x] Title tag pattern → includes role on every page (`Mitul Vaghasiya — Senior Mobile Engineer & Development Lead`) → [src/layouts/Base.astro](src/layouts/Base.astro)

### Micro-copy

- [x] CTAs — `See the work →` → `View work →`, `Read the resume` → `Read resume` → [src/components/home/Lead.astro](src/components/home/Lead.astro)
- [x] Contact form submit — N/A; form was stripped in Phase 1 per 0.7
- [x] `/work` sort label `Order` → `Featured` → [src/pages/work/index.astro](src/pages/work/index.astro)
- [x] Now console header — `//` separator → `·` (cleaner)
- [x] Now `Shipping` subtitle — `B2B diamond trading · iOS + Android` → `Live in app stores · iOS + Android` (decoupled from KGK Diamonds; correct for any KGK shipping project)

### Phase 0 cascade items (rolled in alongside Phase 2)

- [x] Story pull-quote rewritten with Draft A — copy now `Six years, <accent italic>{N} apps shipped</accent italic>, <accent italic>{verified} still live</accent italic> in the stores.` Numbers derived from `getCollection("work")` so they auto-stay-in-sync. Word forms via a `numberWord()` helper. → [src/components/home/Story.astro](src/components/home/Story.astro)
- [x] Numbers stats rebalanced — lead with `Live in app stores` (most verifiable), then `Cities served / Across N continents`, then `Apps shipped`, then `Years in the field`, then `Industries`. Continents now derived via a region→continent map (Asia / North America / Europe / South America = 4 today). → [src/components/home/Numbers.astro](src/components/home/Numbers.astro)

### Phase 2 leftovers (intentionally not touched)

- `src/styles/globals.css` still has dev-only comments (`/* Datasheet stat */`, `/* Specimen hairline */`, etc.) — not visible to users; updating them would be a refactor of class-name conventions, deferred to design pass.
- The case-study CaseStudy.astro `Gallery` heading still says `${N} plates` in its trailing — kept because "plates" here means screenshots, which reads cleanly alongside the per-case-study `Plate XX` numbering.

---

## Phase 3 — Content rewrites

Drafted in chat, approved by you, then committed.

### Home

- [x] Rewrite pull-quote in Story (per 0.8) — landed in Phase 2 with derived counts → [src/components/home/Story.astro](src/components/home/Story.astro)
- [x] Rebalance Numbers stats — landed in Phase 2 → [src/components/home/Numbers.astro](src/components/home/Numbers.astro)
- [x] Decouple "Shipping" subtitle from KGK Diamonds — landed in Phase 2 → [src/components/home/Now.astro](src/components/home/Now.astro)
- [x] Resolve `currentlyLearning` ↔ `skillGroups.PostgreSQL` contradiction per 0.9 — landed in Phase 1 → [src/data/profile.ts](src/data/profile.ts)
- [x] Reconcile "five years at Sadguru Soft" vs "6+ years" framing per 0.20 — landed in Phase 1 → [src/data/profile.ts](src/data/profile.ts)
- [x] Update "Currently learning" list per 0.17 — landed in Phase 1 → [src/data/profile.ts](src/data/profile.ts)

### Case-study Results pass (19 files)

Rewrite Results section per 0.11 + 0.18. **Status: all 19 case studies have had their non-badge Results sections enriched via Round 1+2 link research, anonymity decisions, or the 2026-06-08 narrative polish pass.** Per-file Play Store metric injection still pending (blocked on user-supplied install bands + ratings — see queue item #1).

- [x] [kgk-buy-diamonds.mdx](src/content/work/kgk-buy-diamonds.mdx) — Round 1 metrics + 0.15 metric line applied; pending project-specific crash-free number (open `todos:` line in file).
- [x] [kgk-hrms.mdx](src/content/work/kgk-hrms.mdx) — Round 2 Results enrichment with store-listing positioning.
- [x] [lili.mdx](src/content/work/lili.mdx) — Round 1 metrics + APKPure framing.
- [x] [foresite.mdx](src/content/work/foresite.mdx) — Round 1 metrics + 2026-06-06 agronomist/grower softening.
- [x] [studymap.mdx](src/content/work/studymap.mdx) (formerly remody) — 2026-06-06 rebrand documented in body; brand site link only, store URLs removed.
- [x] [locket.mdx](src/content/work/locket.mdx) — Round 1 metrics, delisting + APKPure framing.
- [x] [lux-ecards.mdx](src/content/work/lux-ecards.mdx) — Round 2 Results enrichment.
- [x] [havitglam.mdx](src/content/work/havitglam.mdx) — Round 2 Results enrichment with store-listing positioning. KR "Largo 라르고" URL still open in `todos:`.
- [x] [tarot-cat.mdx](src/content/work/tarot-cat.mdx) — Round 2 with confirmed MyTarot iOS rebrand (4.7★ / 2,100 reviews).
- [x] [todays-quote.mdx](src/content/work/todays-quote.mdx) — Round 2 Results enrichment, 0.16 mechanic confirmed in body.
- [x] [physio-mobile.mdx](src/content/work/physio-mobile.mdx) — Round 1 metrics.
- [x] [bookmrk.mdx](src/content/work/bookmrk.mdx) — Round 1 with APKPure mirror, status upgrade to verified.
- [x] [bookmrk-delivery.mdx](src/content/work/bookmrk-delivery.mdx) — Round 1 with parent-app framing.
- [x] [marline-media.mdx](src/content/work/marline-media.mdx) — Round 1 with APKPure mirror, status upgrade.
- [x] [synergy.mdx](src/content/work/synergy.mdx) — 0.14 anonymity applied (body already generic); 2026-06-08 narrative polish.
- [x] [setera-tpms.mdx](src/content/work/setera-tpms.mdx) — 2026-06-06 0.14 partially reversed: Setera identity went public; Results updated.
- [x] [warden-tracker.mdx](src/content/work/warden-tracker.mdx) — 0.14 applied; 2026-06-08 narrative polish.
- [x] [careshare.mdx](src/content/work/careshare.mdx) — 0.14 applied; 2026-06-08 narrative polish.
- [x] [radio-station.mdx](src/content/work/radio-station.mdx) — intentionally thin per 0.18 voice + own narrative ("interesting work is everything that came after").

### Per-MDX inline `todos:` cleanup

Each case study has a `todos:` array surfaced as a visible "Editor's notes" amber box. Once content rewrites land, these should be either resolved (deleted) or moved to internal-only notes.

- [x] Walk every MDX, resolve or remove `todos:` entries → 19 files (9 pruned, 10 genuine open items remain — dates, asset gaps, optional decisions)

---

## Phase 4 — Content polish (SEO, metadata, copy)

- [x] Per-page `description` on `/work`, `/resume`, `/contact` — `/work` was already substantive; `/resume` and `/contact` polished from generic placeholders to specific, role-aware sentences (2026-06-08).
- [x] Title tag includes role on every page (also covered by Phase 2)
- [x] Status-badge legend copy — added under the stats grid on `/work`: "Verified live in a public store today · Brand-verified client brand confirmed, store listing delisted or unlisted · Unlisted previously listed, now removed · Internal B2B or staff-only, never on public stores · Early pre-2021 small project." (2026-06-08).
- [x] Set real `site` URL in `astro.config.mjs` and `src/pages/sitemap.xml.ts` — user confirmed 2026-06-08: `https://mitulvaghasiya.com` is the working URL for now. Existing config in [astro.config.mjs:6](astro.config.mjs) and [sitemap.xml.ts:4](src/pages/sitemap.xml.ts) is correct. Revisit if/when a different domain is chosen.
- [x] Verify `/sitemap.xml` excludes `/lab/*` after 0.10 decision — confirmed: `sitemap.xml.ts` enumerates only `["/", "/work", "/resume", "/contact"]` + work slugs by construction. Lab pages excluded.
- [x] CreativeWork JSON-LD — already at `MobileApplication` schema (a `SoftwareApplication` subtype) when an app-store/Play/APKPure link exists; falls back to `CreativeWork` otherwise. Better than spec'd in the original task. → [src/components/CreativeWorkJsonLd.astro:20](src/components/CreativeWorkJsonLd.astro)

---

## Phase 5 — Asset gaps

For each delivered asset: flip `needsAssets: false` in the matching MDX.

- [ ] KGK Buy Diamonds — hero + 3–5 screenshots → `public/work/kgk-buy-diamonds/`
- [ ] KGK HRMS — hero + 2–3 → `public/work/kgk-hrms/`
- [ ] Locket — hero + 3 → `public/work/locket/`
- [ ] Studymap — hero + 3 (JP UI is fine) → `public/work/studymap/`
- [ ] Setera TPMS — any available → `public/work/setera-tpms/`
- [ ] Warden Tracker — any available → `public/work/warden-tracker/`
- [ ] 1200×630 OG PNG for link previews → `public/og-default.png` (replace the SVG that doesn't render on Slack/LinkedIn/iMessage)
- [⏸] Headshot placement — image available in `inputs/`; where/how to show TBD in design pass
- [ ] Optional: 1024×1024 app icons for case-study heroes

---

## Phase 6 — Optional content additions

- [ ] `/uses` page (your tools, IDEs, hardware) — content-only addition
- [ ] Talks / press / writing section if you have any
- [ ] Blog or `/notes` — content collection setup (deferred design styling)

---

## DEFERRED — Design / layout / animation pass

Decisions logged here for when we get to the design phase. **Do not implement these in the current content pass.**

| #    | Decision                                                                                                                | Choice / status                                                                               |
| ---- | ----------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- |
| 0.2  | Hero name-vs-role hierarchy                                                                                             | **Decided: name-first, implementation deferred** ⏸                                            |
| 0.3  | Selected Work layout (sticky carousel vs grid vs hybrid)                                                                | **Open — defer to design pass** ⏸                                                             |
| 0.4  | Ambient effects (drones / scanlines / glitch / scramble / parallax / atmosphere / section FX) — keep all, trim, or cut? | **Open — defer to design pass** ⏸                                                             |
| 0.13 | Home section order                                                                                                      | **Keep current (deferred — current order shares parallax grid-zone across Lead/Now/Story)** ⏸ |

Design-pass open work items (move from Phase 2/4 to here):

- [⏸] Flip hero type hierarchy (name large, role smaller) — [src/components/home/Lead.astro:31-40](src/components/home/Lead.astro)
- [⏸] Reorder home sections — [src/pages/index.astro](src/pages/index.astro)
- [⏸] Audit ambient effects, disable cuts
- [⏸] Hide `BackgroundFX` in print stylesheet
- [⏸] Mobile QA — sticky Picks on iOS Safari, rotator CLS, world map under 375px
- [⏸] Picks layout decision per 0.3
- [⏸] Skills surface decision (searchable on home vs resume)
- [⏸] Status-badge tooltip / hover design

---

## Decisions log

> Append non-trivial decisions with date + reason so future passes don't relitigate.

- _2026-06-05_ — Checklist created from chat audit.
- _2026-06-05_ — **0.1 decided: Keep "Plate 01" numbering on case studies only.** Drop "Specimen Sheets / Datasheet / Catalog / Atlas / Vol. I-III" from nav, page H1s, ribbons, footer, and section headings. Reason: vocabulary overwhelms the name+role on first paint; recruiter speed-scan needs plain labels. Cascade: Phase 2 nav + page chrome edits unblocked.
- _2026-06-05_ — **0.2 decided (impl deferred): Flip hero to name-first.** Name large, role smaller. Implementation moved to design pass because it touches type sizing / layout.
- _2026-06-05_ — **0.13 deferred: Keep current home section order.** Reason: current order shares the `.grid-zone` dot-grid parallax across Lead/Now/Story and reveal staggers depend on the sequence. Reordering = design pass.
- _2026-06-05_ — **Scope tightened: CONTENT ONLY for current pass.** Per user direction, all layout / animation / hierarchy changes moved to a future design pass. Phase 2 now contains only wording/labels/copy. Design decisions (0.3, 0.4) deferred. Phase 4 split — content polish stays, layout polish moved out.
- _2026-06-05_ — **0.9 decided: PostgreSQL is Strong, drop from "Currently learning".** Reason: production usage confirmed. Cascade: `profile.ts` edits — keep Strong in skillGroups Data block; remove from `currentlyLearning` array.
- _2026-06-05_ — **0.17 decided: Keep `currentlyLearning = ["Rust", "Server-side Dart"]` after PostgreSQL is removed.** Two-item list reads as focused; matches GitHub README signal.
- _2026-06-05_ — **0.20 decided: Keep `yearsExperience: "6+"`.** Actual: 6 years 7 months total (5y 4m Sadguru + 1y 3m KGK as of 2026-06). User will manually bump to "7+" once they hit the mark. Sub-decision: tighten `bioLong[0]` "five years" → "5+ years" for precision consistency.
- _2026-06-05_ — **0.5 decided: Pro5 cert URL provided.** `https://drive.google.com/file/d/1O7zn6UVjArLrAM5f_UywToehhe3oCe3-/view?usp=sharing`. Wire into `certifications[0].url`. Heads-up: Drive links can be blocked on some corporate networks — consider mirroring as `/public/certifications/pro5-flutter.pdf` later for max reach. For now, the Drive link is the source.
- _2026-06-05_ — **0.6 decided: Remove phone number from profile.ts entirely.** Reason: not currently shown on the site anyway; leaking to repo scrapers buys nothing. Cascade: delete the `phone` field + TODO comment from `src/data/profile.ts:17`.
- _2026-06-05_ — **0.7 decided: Strip the contact form. Replace with mailto + LinkedIn + GitHub + X buttons only.** Reason: zero backend dependencies, no 3rd-party signups, recruiters prefer email. Cascade: rewrite `src/pages/contact.astro` form section as a button row; delete the Formspree form + TODO banner; mirror the home `GetInTouch.astro` button pattern.
- _2026-06-05_ — **0.10 decided: `noindex` the `/lab/*` pages.** Reason: keep accessible for personal reference but hide from search engines and the sitemap. Cascade: add `noindex={true}` prop to Base layout on each lab page; verify `src/pages/sitemap.xml.ts` excludes them.
- _2026-06-05_ — **0.12 deferred: Headshot.** Image available in `inputs/`. Display placement (which page, sizing, treatment) is a design decision — moved to design pass.
- _2026-06-05_ — **0.19 decided: No promotion dates, but foreground the progression.** Move the "Promoted from Jr → Flutter → Expert → Senior" bullet out of the highlights list and into a strong sentence directly under the role title in `experience[1]`. Copy: "Promoted four times in 5+ years: Jr Flutter Dev → Flutter Dev → Expert Flutter Dev → Senior Mobile App Developer." Renders both in `Track.astro` (home) and `/resume`. Cascade: data model in `profile.ts` may need a new `subtitle` field on `Role`, or just rework the highlights ordering.
- _2026-06-05_ — **0.11 deferred: Case-study metrics.** User to gather Tier 1 (Play Store install band + iOS/Android ratings + review count + last-updated date) for the 10 verified apps + any Tier 2/3 they recall. Then we co-rewrite Results sections. Until then, case-study Results pass in Phase 3 is BLOCKED — do not start MDX edits to Results sections until numbers land.
- _2026-06-05_ — **0.18 decided: Keep current voice — precise technical prose.** Existing voice (see Foresite, Lili) is editorial-but-technical, prose-leaning, reads as a senior engineer who writes. Continue across all 19 rewrites. No bullet-conversion, no narrative-storytelling shift.
- _2026-06-05_ — **0.8 decided: Numbers-led pull-quote.** Final copy: `Six years, <accent italic>nineteen apps shipped</accent italic>, <accent italic>eleven still live</accent italic> in the stores.` Replaces existing `"feel fast and don't fall over"` line in `Story.astro:11`. Two accent-italic emphasis points matches existing pattern. Numbers derived from `getCollection("work")` — same source as Numbers.astro, so they stay in sync.
- _2026-06-05_ — **0.15 decided: KGK Diamonds headline metric = both numbers.** Open the case study with "120-year diamond house, 50,000+ certified inventory." Heritage + scale in one line. Pays off into the engineering narrative about high-volume data grids.
- _2026-06-05_ — **0.16 decided: Today's Quote = leaderboard mini-game tied to quote engagement.** Public-evidence best-guess from store listing. Explains the legacy "2D-Game" tag in the old portfolio. Cascade: update `todays-quote.mdx` Overview/Architecture sections to mention the leaderboard mechanic.
- _2026-06-05_ — **0.14 decided: Per-project anonymity choices.** Synergy → keep product name publicly. Setera TPMS → anonymize as "automotive hardware partner, TPMS sensor utility" (drop `client: "Setera (automotive hardware partner)"` from frontmatter). Warden Tracker → anonymize as "geo-fencing system, B2B client" (drop client-revealing copy from MDX). CareShare → anonymize as "consumer health-care client project" (already has no `client:` field; clean up any name in body copy). Cascade: 4 MDX files updated.
- _2026-06-05_ — **Foresite role: agronomist-facing field tool (default).** Bundle ID `com.ukkoag.enterprise` matches the enterprise agronomist build. Case study to note in passing that Ukko ships a separate grower-facing companion.
- _2026-06-05_ — **Havitglam: cross-link iOS "Largo 라르고" on the case study.** Add iOS App Store URL to frontmatter links once researched.
- _2026-06-05_ — **Tarot Cat: cross-link iOS "MyTarot 마이타로" on the case study.** Add iOS App Store URL to frontmatter links once researched.
- _2026-06-05_ — **Marline Media: link APKPure mirror.** `https://apkpure.net/invitation-card-lenses-maker/com.marlinemedia.invitation.card.maker.filter.mm`. Update status from `brand-verified` → `verified` (now has a working install path), or keep `brand-verified` with APKPure as supplementary. Cascade: research APKPure for all other unlisted/internal apps too.
- _2026-06-05_ — **APKPure-first research heuristic (user-added).** For any app currently flagged `unlisted` / `brand-verified` / `internal`, check APKPure before treating as no-public-link. Candidates to check: Synergy, BookMrk, BookMrk Delivery, CareShare, Radio Station, KGK HRMS (iOS), Setera TPMS, Warden Tracker. Status upgrades possible if APKPure mirrors exist.
- _2026-06-06_ — **Round 1 link research complete (7 apps fetched, metrics now in MDX).** Schema mismatches fixed in 4 files. Status upgrades applied: Marline Media + BookMrk → `verified` (APKPure mirrors confirmed). Real metrics now in MDX for KGK Diamonds, Foresite, Lili, Locket, BookMrk, Marline Media, Physio Mobile. Verified count: 11 → 13.
- _2026-06-06_ — **0.14 partially reversed: Setera identity goes public.** User voluntarily added `setera.seg.br` link. Setera = Brazilian fleet-telemetry hardware company (STR-1010+ tracker, CANBUS, TPMS, driver-monitoring cameras). Updates: status `internal` → `verified`, `client: "Setera"`, added `publisher: "Setera (setera.seg.br)"`, `region: "South America"`, `country: "Brazil"`. Body + Results + Sources updated to name Setera and reference brand site. Verified count: 13 → 14.
- _2026-06-06_ — **Marline Media decision: keep case-study title "Marline Media", mention actual store title "Invitation Card & Lenses Maker" in body.** Already implemented in Results section rewrite.
- _2026-06-06_ — **Foresite decision: soften to neutral (agronomist + grower).** Updated Challenges bullet — replaced strict "agronomist tool" claim with "enterprise build serves both agronomist and grower workflows across the field decision-making cycle." Reason: store-listing description leans grower-facing; bundle ID `enterprise` doesn't pin to one persona.
- _2026-06-06_ — **KGK Diamonds: 95% crash-free / 99.9% uptime kept with replacement TODO.** Added to frontmatter `todos:` array: "Replace generic '95% crash-free / 99.9% uptime' line in Results with a project-specific Crashlytics number when available." Renders as an editor's note on the case study.
- _2026-06-06_ — **Studymap rebranded → Remody.** User confirmed: both store URLs are dead; client renamed the product from "Studymap" to "Remody" mid-life. Status `verified` → `unlisted`. Removed `appStore` + `playStore` from frontmatter `links:`. Kept `liveSite: https://www.remody.co.jp/`. Body rewritten: Overview, Architecture/Distribution, Challenges, Results & impact, Sources all updated to mention the rename and reference the Remody brand site (AI-powered personalised learning plans for cert / uni entrance exams, Tokyo Shinagawa). Verified count: 14 → 13 net. **Open question for user: Studymap is still `featured: true` — should it stay on the homepage Picks now that it's unlisted, or get demoted to /work only?**
- _2026-06-06_ — **Lili: no changes needed.** User confirmed Play Store unlisted; APKPure URL stays as the Android proof. Current MDX already reflects this state correctly.
- _2026-06-06_ — **Studymap stays featured despite unlisted status.** User chose: engineering work stands on its own; brand site link + honest unlisted badge is acceptable framing. Featured count stays at 6 (KGK Diamonds, Lili, Foresite, Locket, Lux ECards, Studymap).
- _2026-06-06_ — **Round 2 enrichment landed.** Tarot Cat got the confirmed MyTarot iOS rebrand link (4.7 ★ / 2,100 reviews — high-impact metric). Havitglam, Today's Quote, Lux ECards, KGK HRMS all got real store-description content in their Results sections. Synergy + Warden Tracker + CareShare anonymity TODOs cleared (decisions already applied; bodies already generic). BookMrk Delivery linked to its parent's APKPure mirror as delisting evidence.
- _2026-06-06_ — **Outstanding fetch gaps.** Play Store install bands + ratings are not extractable via WebFetch (Google anti-scraping); next move is for the user to manually copy install bands + ratings from each Play Store listing in their browser. The few remaining unsurfaced items are: Havitglam KR iOS "Largo 라르고" (not in search), Lux ECards iOS (probably Android-only), Today's Quote iOS (probably Android-only), Lux ECards site (SSL chain error from WebFetch).
- _2026-06-06_ — **Pull-quote number drift flagged.** Draft A says "eleven still live in the stores." After Round 1+2 status changes, verified count is now 14. When Story.astro is updated with Draft A in the Phase 2 sweep, change "eleven" → "fourteen" (or wire to `getCollection("work")` count so it stays in sync).
- _2026-06-06_ — **Phase 1 landed and verified in browser.** Contact form stripped to mailto + LinkedIn + GitHub + X buttons (Reach out panel). Footer year dynamic. Pro5 cert link wired + visible TODO removed. Phone removed from profile.ts. PostgreSQL out of currentlyLearning. bioLong tightened. Promotion sentence rewritten. Lab pages already had noindex meta tags. Sitemap excludes lab pages by construction. Verified at `/contact`, `/`, `/resume` via Claude_Preview snapshots — no errors, copy renders correctly. **One Phase 1 carryover:** user must generate `public/og-default.png` (1200×630) to replace the SVG that Slack/LinkedIn/iMessage can't preview-render — I can't generate PNG files.
- _2026-06-08_ — **Theme toggle now does a cinematic glitch.** User: "make it something like glitch and open. on tap of the toggle each and everything should have the cinematic glitch effect and convert to the light mode and vise-versa." Replaced the View Transitions API circular reveal with a CSS-driven glitch effect. On click: adds `.glitching` class to `<html>` for 640ms, which fires three coordinated layered animations: **(1) Body jitter + RGB chromatic split** — `glitch-jitter 620ms steps(14)` translates the body in unpredictable directions (±5px x/y) while applying drop-shadow filters that ghost the page in pink (`rgba(244,63,94)`) on one side and teal (`rgba(94,234,212)`) on the other; hue-rotate, contrast, and saturate filters add color-glitch flavor at peak. **(2) Scanline overlay** — `html.glitching::before` projects a repeating-linear-gradient of 2px-spaced horizontal lines that shift up/down in 8 step keyframes, mix-blend-mode multiply for CRT-tear feel. **(3) RGB band sweep** — `html.glitching::after` is a linear-gradient strip of magenta+cyan bands that sweeps horizontally across the screen with `mix-blend-mode: screen` for chromatic mis-registration. Theme flips at the **visual midpoint (250ms)** — peak chaos hides the swap. Glitching state reverts after 640ms. Reduced-motion users get the instant theme switch with all glitch animations no-op'd. Verified via stepped DOM eval: `glitching` class on at 100ms, theme flipped + class still on at 300ms, both gone by 600ms.
- _2026-06-08_ — **Theme toggle now has a circular reveal effect across the page.** User: "when we toggle the theme mode it's directly changing the colors instead it should show some cool effects." Rewrote `ThemeToggle.astro` to use the modern **View Transitions API** (`document.startViewTransition`) — on click, the new theme snapshot animates with a `clip-path: circle(...)` that expands from the toggle button's centre to a radius covering the entire viewport diagonal (computed live from `Math.hypot(max(x, w-x), max(y, h-y))`). Custom properties `--theme-x`, `--theme-y`, `--theme-r` set on `:root` so the keyframe knows where to expand from. 620ms duration with cubic-bezier(0.4, 0, 0.2, 1) easing — feels like the new theme "washes over" the page from the toggle. **Fallback for browsers without View Transitions API** (mainly Firefox): adds a `theme-transitioning` class to `<html>` for ~520ms which enables a smooth `transition: background-color/color/border-color/fill/stroke/box-shadow 480ms` on all non-SVG-icon elements. Class is auto-removed after the timeout. **Reduced-motion users** get the instant theme switch with no animation (both paths short-circuited). Verified live: View Transitions API support detected ✓, theme flip works ✓, custom properties set correctly ✓.
- _2026-06-08_ — **Footer content reverted to original 3-column style · gradient mesh background kept.** User: "make it same as before for the content. background is better now so let it be as it is now." Reverted from the editorial-masthead composition (huge centered name + ornament + status strip + horizontal nav strips) back to the previous 3-column grid: Identity (1.5fr — small name + role line + location + Currently status with accent dot), Sitemap (1fr — `→ Home / Work / Resume / Contact`), Reach out (1fr — `→ Email / GitHub / LinkedIn / Twitter`). Bottom strip with copyright + back-to-top kept. Background stays as the static `.footer-mesh` gradient (accent teal glow top-left + sepia warm glow bottom-right + central vignette) + `.footer-bg::before` bottom darkening. Outer padding kept slightly compact (`py-10 md:py-12`) and bottom-strip margin tighter (`mt-10 pt-5`) since the user liked the recent compactness.
- _2026-06-08_ — **Footer animation removed · replaced with static gradient mesh.** User: "leave it. we don't want to show any animations there. we need to show something solid as a background." Removed `import SectionFX`, the `<SectionFX variant="transmit" />` element, and the `--sfx-opacity` inline style. Replaced with a three-layer static gradient mesh applied via a `.footer-mesh` absolute-positioned overlay div: (1) Accent teal glow at top-left (60%×70% radial at 12%/18%); (2) Sepia warm glow at bottom-right (55%×60% radial at 88%/82%); (3) Central vignette darkening corners (80%×110% radial at center, transparent 32% → black/35% at 100%). Footer base gets a slightly darker `bg` color via `.footer-bg`, plus a `::before` linear gradient (transparent → 20% black bottom) for additional depth. No animation. Solid. Visually distinct from any other section because of the corner-glow + vignette atmosphere, while the editorial masthead content (kicker / massive name / role / status line / nav strips / copyright) stays exactly as the user liked it.
- _2026-06-08_ — **Footer redesigned — editorial masthead with massive wordmark.** User: "footer is still not looking that good. check for design options and change the design of the footer to make it more beautiful and different." Replaced the previous 3-column grid (identity / sitemap / channels stacked equally) with an editorial-masthead composition. **Top decorative divider** — hairline rule with centered ✦ ornament in accent teal. **Hero block** — small "GET IN TOUCH" kicker, then **massive display-serif name** at `text-5xl md:text-7xl lg:text-8xl` as the visual anchor, then mono role line, then italic-serif status line with 3 inline pieces separated by accent dots: location · ● Open to senior roles · Currently at KGK InfoTech. **Two compact horizontal strips** — Sitemap (Home / Work / Resume / Contact) and Reach out (Email / GitHub / LinkedIn / Twitter / X), each shown as inline links separated by faint `/` characters; left/right aligned in a 2-column grid. **Bottom strip** — copyright on left ("© 2026 · Mitul R. Vaghasiya · All rights reserved"), back-to-top button on right with arrow that nudges up on hover. Kept the ripple SectionFX background. The name is now unmistakably the visual hero of the footer; everything else condenses around it.
- _2026-06-08_ — **Dot grid + parallax added to "Let's build something" (GetInTouch) section.** Section was previously `<section class="container-page py-24 md:py-36">` — the section element itself was the centered container, which would have constrained a child dot grid to only the padded inner area. Restructured: section is now `<section class="relative overflow-hidden py-24 md:py-36">` (full-width with vertical padding only), with two child layers: (1) `<div class="bg-dot-grid" data-parallax-grid>` spanning full width as the texture; (2) `<div class="container-page relative z-10 text-center max-w-3xl mx-auto">` carrying the content (merged the previous nested div). Shared parallax script picks up the new `data-parallax-grid` element automatically and translates it at 0.55× scroll. Content stays at `relative z-10` so the headline / buttons / footer line render cleanly above the dot grid.
- _2026-06-08_ — **Parallax extended to the dotcircuit canvas in Experience section.** The bg-dot-grid div in Track was already parallaxing at 0.55× (via the shared `[data-parallax-grid]` script in index.astro), but the SectionFX `dotcircuit` canvas on top wasn't — so the two layers would drift apart on scroll. Wrapped the SectionFX in `<div class="absolute inset-0" data-parallax-grid>` so the canvas also parallaxes at the same 0.55× rate. Both layers now move together, keeping the visible dot grid and the circuit pulses locked relative to each other. Track section now has 2 parallax targets (the dot grid + the canvas wrapper), both children of the same `<section data-track>`. The content (timeline + role cards) stays at `relative z-10` with no parallax — appears stationary while the background drifts.
- _2026-06-08_ — **Dot grid added to Experience section · dotcircuit pulses calmer.** Two changes: (1) **Visible dot grid in Track** — added `<div class="bg-dot-grid" data-parallax-grid>` to the Track section so the standard site dot grid is visible as the static layer (with 0.55× parallax matching other dot-grid sections). The `dotcircuit` canvas now layers on top, drawing pulses through its own 44px lattice plus its faint dim dots; the more prominent visible grid comes from the bg-dot-grid below. (2) **Calmer dotcircuit** — spawn cadence slowed from `rnd(70,30)` (30-70 frames) to `rnd(260,110)` (110-260 frames between pulses); max concurrent pulses dropped from 14 to 5; speed is now bimodal — 55% are slow (0.014-0.028), 45% medium (0.032-0.052), no fast pulses. Net: a clearly-readable dot grid as the section's static texture, with much more occasional + calmer current pulses flowing through it.
- _2026-06-08_ — **New `dotcircuit` SectionFX variant for Experience section — current flows through dot grid.** User: "instead of making the circuit on the paths those we've created we will make the current flow through the dot grid things. and this circuit effect will be only for that experience section." Built a new variant that replaces the freeform-path `current` variant in Track: **(1) Static field** — a regular 44px-spacing dot grid covers the entire section (small dim teal dots at 0.22 opacity). **(2) Current flow** — each pulse starts at a random grid intersection, picks an orthogonal direction, and hops dot-to-dot. At each dot reached: 60% chance to continue same direction, 40% chance to turn perpendicular (random left/right). Out-of-bounds = remove pulse. **(3) Visual** — the pulse renders as a trail of line segments connecting the last 10 visited dots, with alpha + width fading from head (bright teal at 0.55, 2.3px) back to tail (transparent). Brightest "active leg" connects the last trail dot to the interpolated head position. Bright white head spark at the tip. Dots in the trail get repainted larger + brighter (1.6-2.8px, accent teal at 0.25-0.80 opacity). **(4) Spawn cadence** — 30-70 frames + scroll-velocity burst, max 14 concurrent pulses. Switched Track from `<SectionFX variant="current" />` to `<SectionFX variant="dotcircuit" />` with `--sfx-opacity: 0.4` (slightly higher than the freeform circuit so the dot grid texture is clearly visible). The old `current` variant stays in SectionFX as available but no longer used.
- _2026-06-08_ — **New `ripple` SectionFX variant created for the footer.** User: "don't want the exact same hex effect. take that as a reference and create something new for the footer similar to that hex field effect." Built a new variant in `SectionFX.astro` that uses the same architectural pattern as `hex` (static repeating field + expanding shockwaves that briefly light up nearby field elements) but with completely different geometry: instead of an angular honeycomb of hexagons, it's a staggered grid of tiny round dots (38px spacing, columns offset by half-step for an organic non-rigid feel). Each cell is a 1.1px base dot at 0.18 dim opacity. When an expanding ring pulse passes through a dot, the dot grows up to 2.9px and brightens to accent teal (up to +0.55 alpha). Ring spawn cadence: 70–150 frames + scroll-velocity burst. Softer / more ambient than the angular hex variant — better for a quiet end-of-page section. Switched Footer to `<SectionFX variant="ripple" />` with `--sfx-opacity: 0.32`. Added `ripple` to the SectionFX Props type.
- _2026-06-08_ — **Hex pulse field moved from Skills to Footer.** User: "we don't need to add there. we need to add the things in the footer." Reverted Stack.astro (dropped `import SectionFX`, the SectionFX element, the `--sfx-opacity` style, and the `relative overflow-hidden` / `relative z-10` scaffolding) — back to plain `border-y border-border bg-paper/40` section. Added the same hex pulse setup to Footer.astro: imported SectionFX, made the `<footer>` `relative overflow-hidden` with `--sfx-opacity: 0.18`, slotted `<SectionFX variant="hex" />` as the first child, and put `relative z-10` on the inner container so the footer text/links render above the canvas. Same minimal "faint honeycomb + occasional accent shockwaves" effect, just behind the footer content now.
- _2026-06-08_ — **Minimal hex pulse field added to Skills (Stack) section.** User asked to add a hex-pulse-like background, but minimal so content stays readable. Added `<SectionFX variant="hex" />` to the Stack section with `--sfx-opacity: 0.18` (much lower than the default 0.30) — gives a faint honeycomb static grid (dim hexagons at 0.13 × 0.18 ≈ 0.02 visible opacity) plus occasional expanding shockwaves that briefly light up nearby hexagons in accent teal as they pass through. Section gets `relative overflow-hidden`; inner container gets `relative z-10` so skill chips sit above the canvas. Picked Stack because it was the most visually bare content section after the dot-grid removal, and the hex-as-building-blocks visual pairs thematically with "what I build with."
- _2026-06-08_ — **Footer audit + restructure.** Removed dead-weight content, added real utility. **Dropped:** "Doc rev. 2026.06" (resume-context only, confusing in footer), "Set in Fraunces & Inter" (typographic pedantry), "Built with Astro" (tech credit better placed in README). **Added:** (1) Role line under the name (`Senior Mobile Engineer & Development Lead`) so recruiters see what Mitul does even at the bottom; (2) "Currently" status line with accent dot — `● CURRENTLY — Mobile lead at KGK InfoTech`; (3) Sitemap navigation column (Home / Work / Resume / Contact) — utility for users at the end of a long scroll; (4) Bottom strip with copyright + a smooth-scroll Back to top button. **Cleaned up:** Channel links now show as compact labels (`→ Email`, `→ GitHub`, `→ LinkedIn`, `→ Twitter/X`) instead of the full long ugly URLs — destinations are the same. Three-column grid: identity (1.5fr) / sitemap (1fr) / channels (1fr). Bottom strip is full-width with a horizontal rule above it.
- _2026-06-08_ — **Picks dot-grid bleed into Track/Stack fixed.** User reported the dot grid was still visible on the experience and skills sections even after the Skills removal. Root cause: when scrolling, the Picks section's `.bg-dot-grid` div translates downward by up to `0.55 × sectionHeight` (~600px) via the parallax script. Because `.picks` had no `overflow: hidden`, the parallax-shifted grid would extend below the section into Track and Stack, making it look like those sections had a dot grid. Fix: added `overflow: hidden` to `.picks` so the parallaxing grid stays clipped within the section. The Lead/Now/Story area was already correctly clipped via `.grid-zone { overflow: hidden }` in globals.css. Verified `window.getComputedStyle('.picks').overflow === "hidden"`.
- _2026-06-08_ — **Dot-grid removed from Skills (Stack) section.** User: "remove the dot grid from the experience and skills sections." Track (experience) never had a dot grid in the first place — it uses the `current` SectionFX variant (electrical flow). Stack (skills) had the shared `bg-dot-grid` added one turn ago; that has now been removed. Cleaned up the `relative overflow-hidden` on the section and the `relative z-10` on the inner container since those were only there for the dot grid. Skills section is back to its plain `border-y border-border bg-paper/40` look. Remaining dot-grid usage on the home page: Lead/Now/Story area (via `.zone-grid`) and Picks (Selected Work).
- _2026-06-08_ — **Dot-grid unified across the home page · visibility bumped · stronger parallax.** User: "check for the dot grid and increase the visibility and make the common where we are using and make sure to give the more parallax effect." **(1) Visual unification:** added a canonical `.bg-dot-grid` utility class to `globals.css` that everyone uses. Same colour (`var(--grid)` at 0.55 opacity, up from 0.32-0.38), same dot size (1.4px, up from 1.2px), same 26px × 26px spacing. The legacy `.zone-grid` was bumped to match (0.38 → 0.55, 1.2px → 1.4px). **(2) Picks consolidation:** removed the inline `.picks::before` pseudo with its own slightly-different values; replaced with `<div class="bg-dot-grid" data-parallax-grid>`. Kept the top/bottom mask fade locally for Picks. **(3) Stack consolidation:** removed `<SectionFX variant="dots" />` (canvas wave dots — a different visual entirely) and replaced with the same `<div class="bg-dot-grid" data-parallax-grid>` so all three home regions (Lead/Now/Story area, Picks, Stack) now share the identical dot pattern. **(4) Stronger parallax:** rewrote the inline parallax script in `index.astro` to be generalised — it now finds `.zone-grid` AND every `[data-parallax-grid]` element and applies parallax to each based on its parent section's scroll position. Multiplier bumped from `0.3` to `0.55` (nearly doubles the depth effect). Single rAF loop handles all grids. Reduced-motion still short-circuits everything.
- _2026-06-08_ — **Current-flow direction inverted to top-right → bottom-left.** User: "just invert the flow. make the top-right to the bottom-left." Two changes in the `current` SectionFX variant: (1) start positions moved from random-within-cell to **upper-right** of each grid cell (`x = w*(cx + 0.6 + rand*0.35)/cellsX`, `y = h*(cy + 0.05 + rand*0.35)/cellsY`) so all 9 paths start in the top-right region of their cell; (2) segment direction biased — horizontal segments now use `rnd(60, -350)` (mostly leftward with light right-variance), vertical segments use `rnd(280, -60)` (mostly downward with light up-variance). Net effect: every path now flows from top-right toward bottom-left with enough variance to feel organic, not mechanical. No changes to spawn cadence or trail rendering.
- _2026-06-08_ — **New SectionFX `current` variant — full-section circuit with flowing current trails (not dots).** User feedback on the `circuit` variant: paths felt biased to top-left to bottom-right, and the moving glowing dots didn't read as "electrical current" — wanted actual current flowing through the wires. Built a new variant in `src/components/SectionFX.astro`: **(1) Full-section coverage** — paths distributed via a 3×3 grid; each grid cell seeds one path with random offset inside the cell, so 9 paths span the entire canvas evenly (no spatial bias). Each path has 5-8 orthogonal segments wandering ±320px horizontal / ±240px vertical. **(2) Flowing current visual** — replaced the discrete dot pulse with a `TAIL = 60px` glowing line-segment trail that follows the path's geometry (precomputed segment lengths + `posAt(path, d)` walker). The trail renders as 14 sub-segments with alpha + line-width fading from bright head (`1.5 + 2.5px`) to transparent tail (0), so the pulse reads as a luminous "current zone" flowing through the wire. Bright white head cap at the leading edge for sharp signal. **(3) Spawn cadence** — periodic (every 18-55 frames) + scroll-velocity burst (when |scroll v| > 7, extras spawn up to a cap of 18 concurrent pulses). Switched Track to `<SectionFX variant="current" />` with `--sfx-opacity: 0.32`. Added "current" plus all the other implemented variants (contour, constellation, aurora, grid) to the Props type which had been incomplete.
- _2026-06-08_ — **Track circuit moved from timeline-only to whole-section.** User: "you are again and again only sticking with the vertical line divider that we've created to separate the experience and title. instead I want the things to flow in the entire section." All three of my prior attempts (single pulse, three pulses, SVG circuit) anchored to the left timeline divider only. Replaced with `<SectionFX variant="circuit" />` on the whole Track section — the canvas-based effect draws 8 random orthogonal circuit traces spanning the full section width and height, with bright pulses racing along path segments. Set `--sfx-opacity: 0.18` (lower than the default 0.30) so the circuit reads as subtle ambient activity, not foreground. Content gets `relative z-10` to sit above the canvas. Kept the role-marker flicker animation since it pairs naturally with passing circuit pulses. Removed all the previous custom SVG markup and `.trc` / `.track-circuit*` CSS — the SectionFX canvas handles the visuals now.
- _2026-06-08_ — **Track electricity changed to SVG circuit-board pulse traces.** User: "try different circuit flow." Replaced the three vertical CSS pulses with an SVG circuit drawn in the timeline gutter (30px wide, full ol height, non-preserving aspect ratio). Two SVG layers: (1) **base etched traces** in muted-border at 55% opacity — a vertical spine + 6 branches at varied heights (horizontal stubs and L-shapes) + terminator dots at branch ends, always visible as the "circuit board"; (2) **pulse overlay** — same 7 paths in accent teal with `drop-shadow` glow, each using `stroke-dasharray` with a short visible segment + long gap so animating `stroke-dashoffset: -1000` makes a single bright pulse race along the trace and disappear. Prime-number durations (5.3 / 6.1 / 6.2 / 7.1 / 8.7 / 4.7 / 11s) + per-trace negative delays prevent sync; pulses arrive at different points constantly. Spine has slow long pulse; branches get quick zips. Kept the role-marker flicker (pairs well — markers occasionally arc when branch pulses pass nearby). Reduced-motion safe.
- _2026-06-08_ — **Track electricity made random + multi-source.** User: "should be random and subtle instead of just the single flow line." Replaced the single pulse with three pulses on prime-number durations (13s / 19s / 17s) so they never sync into a pattern — pulse #1 falls (accent, 3×28px), pulse #2 falls smaller + dimmer (accent at 55% opacity, 2×18px), pulse #3 RISES (sepia at 70% opacity, 2×22px, slight horizontal offset). Each pulse uses different easing (cubic-bezier / ease-in-out / linear) and starts at a different point in its cycle via negative `animation-delay`. Added a subtle role-marker flicker — `.track-node` boxes pulse `box-shadow` glow on a 7.3s cycle, each marker getting `--node-delay: ${i * 2.7}s` so adjacent markers flicker offset. Reduced-motion still disables everything. Net feel: faint random activity along the wire rather than a single metronomic streak.
- _2026-06-08_ — **Minimal electricity pulse added to Track timeline.** User asked for a "minimal effect of electricity passing" on the experience section, explicitly low-attraction so content stays the focus. Implemented as a pure CSS animation: a 3×28px accent-teal vertical streak with a 0.55-opacity drop-shadow glow that travels top→bottom along the timeline left edge over 11s. Fades in at 4% of the cycle, fades out at 96%, then loops. The `<ol class="track-timeline">` got `position: relative` and a faint `::before` connector line (45% opacity border) so the pulse appears to travel on a continuous wire (filling the gaps between role markers where there's no per-li border). Section gets `overflow-hidden` so the pulse can't render past the section bounds; ol kept overflow-visible so the -5px role markers stay visible. Reduced-motion safe (`@media (prefers-reduced-motion: reduce)` disables the animation). No JS — pure CSS, runs continuously but cheap (single transform-only animation).
- _2026-06-08_ — **Atlas section cut from home page · dot-grid FX moved Track → Stack.** User: "is it even required?" — agreed the Atlas section wasn't earning its place (geographic distribution is a weak signal for senior mobile engineering recruiters, the info is duplicated in /work and /resume, and we'd burned many iterations without it landing). Changes: (1) removed `import Atlas` and the `<Atlas />` render from `src/pages/index.astro`; (2) deleted `src/components/home/Atlas.astro` since it became orphaned (WorldMap component stays — still used on /work page); (3) moved `<SectionFX variant="dots" />` from Track to Stack; cleaned up Track to no longer be `position: relative` or carry the SectionFX import; added `relative overflow-hidden` to Stack section plus `relative z-10` on its inner container so the dot-matrix canvas renders behind the skills chips. Home page is now 8 sections (was 9). Verified: Track HTML no longer contains `<canvas>`, Stack does, `Atlas` is fully gone from rendered home HTML.
- _2026-06-08_ — **Atlas variants lab — bugs fixed after live inspection.** User: "there are some issues in the styles. the variations looks off and broken." Inspected each variant via DOM-show-only-target eval + screenshot. Identified and fixed: (1) **D — Editorial Postcards was unreadable** — `rgb(var(--paper) / 0.95)` resolves to dark blue-gray in dark theme so dark text on dark cards. Replaced with hardcoded cream `#ece4d1` / dark `#2a2418` palette so postcards always read; (2) **E — Timeline cards collide at 2022** — three cities (Toronto, Tokyo, Seoul) all started 2022 and their 200px cards stacked on the same point. Added `yearGroups` precomputation; within each year group, cards now alternate above/below the rail AND offset horizontally ±28px from the year tick; 3+ cards stack vertically with 110px offset increments. Also changed the timeline to use `byStart` instead of mutating `hubs` (was breaking downstream variants); (3) **B — Globe wireframe missing 3 of 5 cities** — orthographic projection centered at 80°E hid Toronto on the back side. Re-centered at 30°N 30°E, lowered visibility threshold to -0.6 with opacity falloff, so all 5 cities show (Toronto at west edge, Tokyo at east edge); also fixed broken latitude/longitude line math (proper trig now); (4) **A — Sort order put Seoul before Surat (home)** — added home-first tiebreaker to the sort; Surat now always #01 with HOME badge regardless of count ties; (5) **A — Seoul/Tokyo labels overlapped on map** — Seoul label now offset above the dot, Tokyo stays at default; (6) **C — Constellation connection lines crossed messy** — now uses `byLng` so connections trace a clean west→east trail.
- _2026-06-08_ — **Atlas section visual variations lab at `/lab/atlas-variants`.** User: "build visual variations" for the Atlas section. 8 fully-rendered alternatives, each using the actual city/country/count data: (A) Tactical Console Refined — SVG with lat/lng grid, country ghost labels, fixed Seoul/Tokyo overlap via offset positioning, home-base ring + "HOME" badge on Surat, stat strip; (B) Globe Wireframe — orthographic-ish SVG globe centered on India with longitudinal+latitudinal grid, cities as glowing dots; (C) Constellation Sky — pure black background, no continents at all, cities as named stars connected by dashed lines, decorative background stars; (D) Editorial Postcards — 5 paper-stock cards on a pinboard with random tilts, postage-stamp graphic per city, postmark-style date label; (E) Timeline Strip — horizontal year axis 2019-2026 with city pins above/below alternating, line drops to each card; (F) Compact Card Grid — uniform 5-up cards with country code badge, oversized count number, city/tagline; (G) Coordinate Datasheet — no map, pure technical table with lat/lng coordinates and ◆ home indicator; (H) Subway Transit Map — SVG stylized transit with colored category lines (Enterprise red, Consumer teal, AgTech sepia) connecting cities as stations with legend.
- _2026-06-08_ — **Picks card click model rewired — mystery hover area removed, CTA made tappable.** User reported two issues: (1) "READ CASE STUDY button is not tappable" and (2) "between the content and image there is some tiny hoverable space and on tap of that it's redirecting to the specific case-study" — both symptoms of the same problem. The card had a `.pick-link` invisible full-card overlay at z-index 1 (catching clicks in the gap between image and text) AND the `.pick-cta` "Read case study" was just a `<span>` sitting inside `.pick-actions` at z-index 3 (above the overlay, so clicks didn't fall through to the link). Refactor: removed the `.pick-link` overlay entirely; made `.pick-shot` an `<a>` (image is now a real anchor); wrapped `.pick-title` text in an `<a>` (title is a real anchor with its own hover-to-accent); changed `.pick-cta` from `<span>` to `<a>` (CTA is a real link with arrow-slide hover + color shift). Removed the `onclick="event.stopPropagation()"` hack on `.pick-store` (no longer needed since there's no overlay to bubble into). Net result: three explicit clickable targets (image, title, CTA), each with its own focus/hover state for keyboard accessibility. No invisible mystery click area.
- _2026-06-08_ — **Picks Museum Plate header trimmed — rules removed, top spacing tightened.** User after seeing the live integration: "remove the top and bottom lines and decrease a space lil bit from top." Stripped both `.picks-plate-rule` divs (top and bottom double-hairlines) from JSX and removed their CSS. Reduced `.picks` section top padding from 6rem/8rem (mobile/desktop) to 3rem/4rem. Removed the 2.5rem margin-top from `.picks-plate-overline` since it no longer needs to clear the rule. Reduced `.picks-plate-frame` padding-top from 2rem to 0.5rem. Net effect: header now flows directly from the Numbers section above with much less vertical breathing room, and the composition is just the centered overline / H2 / meta / ornament — no framing rules.
- _2026-06-08_ — **Picks header swapped to Variant J — Museum Plate (centered exhibition wall text).** User picked J after reviewing all 14 lab variants. Stripped the previous split-layout-with-Contents-TOC + the top ribbon — Variant J is meant as a self-contained centered moment. New header structure: double hairline rule top → mono uppercase overline "PLATES 01 — 06" in sepia → display-serif H2 `A few I'm proud of.` (centered, weight 500, italic-leaning size) → italic-serif meta line "Featured projects · 2019 — 2026 · case-study format" with the `All 19 projects →` link folded in (mono micro-caps, accent color, underline) → accent ✦ ornament → double hairline rule bottom. Mobile: meta line stacks, drops the dot separator. Functional `All N` link preserved without sacrificing the museum-plate purity. Old CSS for `.picks-ribbon`, `.picks-heading`, `.picks-heading-grid`, `.picks-kicker`, `.picks-h2`, `.picks-sub`, `.picks-toc*` removed cleanly. Verified live on `/`.
- _2026-06-08_ — **Picks header lab extended to 14 variants — added 7 photography-portfolio inspired (H–N).** User: "show me some more variants where the ui should be like photography portfolio." Added: (H) Contact Sheet — 3×2 thumbnail grid with passe-partout borders and print-style corner ticks; (I) Editorial Spread — magazine layout with oversized framed hero photo left + italic-serif title block right; (J) Museum Plate — centered exhibition wall text framed by double hairline rules + ornament; (K) Catalogue Card — library card aesthetic with mono dashed/dotted dividers, FEATURED stamp rotated -8°; (L) Italic Centered — single line of italic display serif, generous whitespace, hairline divider; (M) Film Negative Strip — black film strip with sprocket holes top and bottom containing 6 frames; (N) Magazine Masthead — Vol/Issue + Edition framing rules with accent-teal end dots. Index nav at the top of the lab now groups variants by source: "Originals" (A–G) and "Photography portfolio" (H–N). The original 7 stay so user can compare against the photography aesthetic.
- _2026-06-08_ — **Picks header lab: 7 visual variants for comparison at `/lab/picks-header-variants`.** User wanted to see options after the previous header rewrite still felt off. Built a noindex lab page with 7 distinct top-half compositions each rendered in real visual context (dot-grid backdrop, actual featured-work data): (A) Cinematic Billboard — centered, stats strip; (B) Giant 06 Asymmetric — massive accent-teal "06" left, headline stacked right; (C) Datasheet / Spec — technical key-value rows matching the resume Subject Metadata block style; (D) Console / Terminal — `$ open --section selected-work --depth full` with blinking cursor, plays into the cyberpunk monospace aesthetic; (E) Filmstrip Thumbnails — tiny hero previews as visual TOC; (F) Manifesto Quote — large italic pull-quote "Six of nineteen worth the deep read."; (G) Vertical Stacked Banner — all-caps display type, three lines staggered with indent, framed by rules. Sticky variant-tag bar at the top of each section so the user always knows which is which. Anchor nav at top jumps between letters. The live Picks section's current header stays in place; once the user picks a letter, I'll swap that variant in.
- _2026-06-08_ — **Picks header redesigned — split layout with editorial Contents TOC on the right.** User: "this part is yet not looking good and need to make it more attractive." Problems identified: H2 "Selected work." duplicated the ribbon's "SELECTED WORK · 06 PICKS", lots of dead space on the right of the headline, kicker "A few I'm proud of" and sub were saying similar things. Restructured: kicker now `[dot] FEATURED · 06 OF 19` with a small glowing teal dot, H2 promoted from kicker text to `A few I'm proud of.` (more personal headline), sub tightened to one editorial sentence. New right column is a Contents TOC — vertical 6-item list with: numbered prefix (mono accent), project title (display serif), category (mono micro-caps). Each row is a hash-anchor link to `#pick-N`, with `padding-left` slide on hover plus accent color shift. Border-left rule on the TOC column visually anchors it as a sidebar. Added `id="pick-N"` to each pick article so the TOC links work. Mobile: TOC stacks below the headline, drops the category meta column. Eliminates the previous headline-bottom-half dead space and gives the section an editorial/magazine-TOC feel that previews what's coming.
- _2026-06-08_ — **Dot grid backdrop added to Picks section.** User: "add the dot grid in the background for this section." Used a `.picks::before` pseudo-element with `radial-gradient` matching the site's existing dot-grid convention (color `var(--grid)` at 32% opacity, 1.2px dots at 26px × 26px spacing — same as the home's `.zone-grid` over the first three sections). Top and bottom 14% fade via `mask-image: linear-gradient` so the grid doesn't fight adjacent sections (Numbers above, Track below). Sits at `z: -1` behind every pick element with `pointer-events: none` so it never interferes with hover or click. Section gets `isolation: isolate` so the pseudo stays contained.
- _2026-06-08_ — **Picks hover lag fix + scroll parallax on watermark numbers.** User feedback: full-card hover background tint was "distracting and not properly visible, gives the feel like lag" + wanted scroll parallax on the numbers. Two targeted fixes: (1) Removed the `.pick-link` hover background tint (`rgb(var(--paper) / 0.06)`) and its 220ms transition entirely. The full-card hit area is now invisible at all times — hover affordances come from the image lift, corner-tick fade-in, title color shift to accent, CTA arrow slide, and watermark deepening. (2) Added a scroll-parallax IIFE script that translates `.pick-num` watermarks at 0.22× the scroll velocity, opposite direction, capped at ±90px so the number can't escape into adjacent sections when picks are far off-screen. Numbers feel "anchored slower" than the content — classic parallax depth. rAF-throttled, single scroll listener for all 6 picks, `prefers-reduced-motion` short-circuit. Verified initial paint shows all numbers at cap (-90px) when picks are below viewport — they smoothly transition through 0 to +90 as each pick crosses viewport center.
- _2026-06-08_ — **Picks section redesigned as clean editorial flow — scroll-jacking removed entirely.** User feedback after iterating through 60+10+20+20 animation variants: "forget what I've asked. just redesign this part with some better styles and design." Stripped the sticky pinned journey, SectionFX warp background, IntersectionObserver scroll-engine, and all the cinematic place-function JS. New design: 6 featured projects laid out as a vertical editorial flow with alternating image-text layout (image right on odd rows, image left on even rows). Each project has: a big mono number watermark (clamp 5-9rem at 8% accent opacity), framed hero image with hover lift + corner-tick accents that appear on hover, large display-serif title that shifts to accent on hover, category badge + status badge, one-liner, spec grid (Role / Stack / Distrib.), Read case study CTA with sliding arrow + StoreLinksRow. Full-card hit area links to case study, with StoreLinksRow protected from bubble. On-scroll reveal uses the site's existing `.reveal` pattern. No JS — pure CSS hover affordances. The lab page at `/lab/picks-variants` remains for reference if needed (still noindex). Verified via HTML inspection: 6 pick articles, all elements present, no journey script residue, no SectionFX in section.
- _2026-06-08_ — **Picks lab redesigned with sequential timing + 20 action-cinema variants.** User feedback on prior 60: cards overlapped during transitions causing content conflicts and visibility issues; also wanted action-cinema effects, not tame fades. Replaced all 60 with 20 new variants using a strict sequential timing model: each card has its own scroll window `lp ∈ [0, 1]`, with phases ENTER `[0, 0.22]` → HOLD `[0.22, 0.78]` → EXIT `[0.78, 1.0]`. Cards outside their window are fully hidden. At any scroll position, at most ONE card is on screen (briefly zero during the cut between cards — film-cut feel). The 20 new variants: (01) Smash Cut + Shake, (02) Whip Pan with 40px motion blur, (03) Bullet Time (slow-mo from -1500px depth), (04) Glitch Cut RGB chromatic split, (05) Vortex Spin 720°, (06) Light Burst with accent flash overlay, (07) Wormhole iris+depth, (08) Holographic Crack RGB ghosts, (09) Shutter Snap camera shutter, (10) Speed Lines anime-style, (11) Camera Punch-In 0.3→1 with impact shake, (12) Pixel Materialize with contrast, (13) Mirror Shatter polygon star clip-path, (14) Title Sting directional motion blur, (15) Light Beam Scan with overlay beam element, (16) Drone Flyby 90° Y-rotation, (17) Earthquake Hold with continuous CSS shake, (18) Solar Flare radial sepia+teal burst, (19) Time Warp Trail with stacked drop-shadow after-images, (20) Punch-Through Camera (Z -1800→0→+1500). New CSS overlay elements: `.pkv-flash` for light bursts, `.pkv-beam` for scan beam, `.pkv-quake` continuous shake animation, `.pkv-speedlines` pseudo-element streaks. Each card uses random `d.dir` and `d.angle` so adjacent cards in the same variant take different directions/spins (the user's "random effects" requirement). Step height upped to 100vh per card for more breathing room. Sequential timing verified via DOM eval at p=0.5/0.95/1.0/1.05/1.5/5.5 — only one card visible at each point, brief gap between cards.
- _2026-06-08_ — **Selected Work animation lab expanded to 60 variants.** User asked for 50 more on top of the original 10. Added 5 new waves (slides / scale-depth / masks / 3D-filter / composites) with 10 variants each. New variants 11-60 include: slide variants (mirror, diagonal, long-pan, slow-drift, snap, rotate, tilt-3D, stack-push, reverse-pan), scale/depth (reverse-dolly-zoom, pure zoom-in, spiral, pinch, z-dive, layered-parallax, heart-beat, spring-elastic, telescope, macro), mask/clip-path (diagonal wipe, vertical wipes both directions, iris from corner, diamond, center slit, venetian blinds, triangle, curtain drop, hexagon), 3D/filter (Y-flip, X-flip, cube rotation, tilt scan, wave warp, skew slide, light streaks via brightness, hue shift, mono→color, hard cut), and composites (slide+iris, parallax+DOF, vertical-dolly+zoom, crossfade+rotate, iris+scale, wipe+skew, stack+flip, chromatic split, time freeze, random-per-card). Total 60 sections × ~70vh each = ~42 viewports of scroll. Anchor nav at the top lets the user jump directly to any variant.
- _2026-06-08_ — **Selected Work animation variants lab page shipped at `/lab/picks-variants`.** User asked: "show me 10 variants of the selected work section with different styles of card animations on scroll and background effects." Built a noindex lab page with all 10 variants pinned-and-scrollable, each with its own card transition formula + background style: (01) Camera Pan + Focus Pull, (02) Vertical Dolly, (03) Iris In/Out, (04) Anamorphic Bars, (05) Dolly Zoom Vertigo, (06) Crossfade Black, (07) Parallax Layers, (08) Reveal Wipe, (09) Soft Push-In, (10) 3D Tracking Shot. Each section is a 70vh × N journey with its own `place(card, lp, d, w, h)` function (10 distinct math formulas). Backgrounds: warp starfield, warp + cinema letterbox, aurora, dots, grid, vertical gradient, black + vignette, black + center glow. Anchor-nav at the top lets the user jump between variants. Reduced-motion safe (cards fall back to in-flow stack). Page is noindex per lab convention. User can pick a number and I'll wire it into the home page Picks section.
- _2026-06-08_ — **Selected Work cinematic pass v2 — pivoted from busy background to subtle camera-pan.** User feedback on v1: "this looks more worse. I want some subtle effects but more cinematic while scrolling." Reverted v1's heavy additions (meteors, warp pulse, cinematic vignette, scan-flare) back to the original calm `warp` starfield. **Replaced the random-angle fly-in/fold-spin card motion with a cinematic camera-pan model:** all cards now slide in from the right with focus-pull (12px blur → sharp), hold center with a tiny breath-drift (±0.9% × W/H), and slide out to the left with defocus (sharp → 12px blur). Easings tuned for cinema (ease-out quart entering, ease-in power exiting — long anticipation → fast resolution). Z-depth pulls forward on entry (−240px → 0) and recedes on exit. Subtle tilt (±0.5°) and vertical drift (±6%) per card so transitions feel like a real camera, not a stopwatch. Added Ken Burns slow-zoom (scale 1.02→1.06→1.02 over 18s with drift) on the held card image — each card has a staggered animation-delay so the six don't sync. Reduced-motion safe. Verified mid-transition states via journey debug handle: at p=2.5, card 2 exiting at opacity 0.54 / 3.7px blur, card 3 entering at opacity 1.0 / 0.8px blur — clean simultaneous pan.
- _2026-06-08_ — **Selected Work section made cinematic.** User asked for a "going-through-space + meteors from different directions" feel on the Picks section. Enhanced the `warp` variant in `SectionFX.astro`: (1) **Depth-tiered starfield** — every star now has a `z ∈ [0..1]` depth value; far stars move slower & dimmer, near stars faster & brighter, producing real 3D parallax. (2) **Meteor streak system** — bright streaks spawn from 5 random off-screen edges (top, top-left/right diagonals, sides), each with a fading line trail, soft head halo, and bright core. Mostly white, occasional teal, rare sepia. Spawn cadence shortens with scroll velocity. (3) **Warp pulse on card transitions** — `Picks.astro` now signals `window.__sfxCardIdx` whenever a new card becomes the centered "held" card; the canvas detects the change and fires a brief speed-burst (drive boost) + spawns an extra teal meteor + paints a soft radial brightening across the screen. (4) **Cinematic vignette overlay** — new `.pk-vignette` div renders only inside `.is-journey`, dark radial corners + soft top/bottom bars frame the action like a viewfinder. (5) **Reactive scan-flare** — new `.pk-scanflare` div with a wide horizontal accent gradient (mix-blend: screen) that brightens with scroll velocity (`--pk-flare` set dynamically) and drifts side-to-side via 9s alternate animation. Reduced-motion safe throughout. Verified via canvas pixel analysis (0.50% bright pixels, teal + white meteor traces detected); preview harness can't capture motion in a single screenshot, but real browsers will show the full dynamic. Side benefit: the `/lab/project-variants` page (which uses the same `warp` variant) now inherits the enhanced effect for free.
- _2026-06-08_ — **Resume.pdf nav tab + `/resume/view` page reverted.** User decision: "remove the Resume.pdf tab. we already have the resume tab and that is good enough." Deleted `src/pages/resume/view.astro` and the empty parent directory. Removed Resume.pdf nav item from both desktop and mobile nav. Reverted the `isActive` exact-match workaround back to original `startsWith` since the /resume/view conflict no longer exists. Cleaned the now-unused `profile` import from `Nav.astro`. Net state: nav back to 3 links (Work / Resume / Contact) + theme toggle on desktop; same three + hamburger on mobile. The `/resume` page's existing Download PDF button covers the artifact path.
- _2026-06-08_ — **Nav header overhaul landed.** Fixed the critical mobile UX gap: nav links were `hidden md:flex` with no fallback below 768px, leaving recruiters on phones stranded with just logo + theme toggle. New header now has: (a) hamburger button on mobile that opens a slide-down sheet containing all nav links + a Resume.pdf download; (b) dropped the explicit "Home" link from the desktop nav since the logo already handles it (industry convention for personal portfolios); (c) added a `↓ Resume.pdf` quick-download in the desktop nav for recruiter convenience; (d) stronger active-page indicator — small accent bar under the active link on desktop, accent left-border on mobile. Hamburger transforms to X on open; closes on Escape, link click, or X tap. ARIA expanded/controls wired correctly.
- _2026-06-08_ — **Resume freshness + leadership signals added (round 2).** Added `Now shipping: KGK Buy Diamonds + HRMS` as a new row in the `Subject metadata` SpecBlock — gives recruiters concrete current-product context at first glance. Swapped the `Industries` stat tile for `Promotions: 4 / Jr → Senior in 5 yrs` — sharper leadership-trajectory signal than "16 categories" (which was mathematically expected and didn't communicate much). Cleaned up the now-unused `regions` and `industries` Set computations in `resume.astro`. All verified via HTML eval; no server errors.
- _2026-06-08_ — **Resume content + UI rewrite (round 1) landed.** Senior-recruiter-level critique applied to both PDF and web `/resume`. Web-side changes: `profile.ts` — rewrote `bioShort` (now leads with KGK 50,000+ inventory specifics + 19/13 apps numbers), tightened `bioLong[0]` to name KGK products. Rewrote both role highlight arrays — KGK lead now names the actual products shipped (Buy Diamonds, HRMS), Sadguru now names the international clients per market (Foresite/Lili/Studymap/Havitglam/etc.). Recalibrated skill levels — dropped "expert" claims on Kotlin, Swift, Java, MVVM, SOLID to "strong"; moved SwiftUI/UIKit/Jetpack Compose/KMP from "strong" to "familiar". Added new "AI dev tools" skill group (Cursor / Claude Code / GPT), removed the same from "Tools & DevOps". Trimmed `Subject metadata` block on `/resume` — dropped duplicate email/github/linkedin (already in the bottom Channels block), added `Open to: Senior IC · Lead · Architecture` as a positioning signal. PDF-side changes still needed from user: rewrite the Professional Summary, both role bullet sets, the Promoted-line framing, drop the unattributed 95% crash-free claim — drafts provided in chat. Also Pro5 cert standalone line, separate AI tooling line, and Testing & QA decision still PDF-only follow-ups.
- _2026-06-08_ — **Site URL confirmed as `https://mitulvaghasiya.com` for now.** User confirmed the existing config in `astro.config.mjs` and `sitemap.xml.ts` is correct as the working URL. No code change needed; status flipped from "blocked" to "confirmed (revisit on domain change)."
- _2026-06-08_ — **Custom 404 page + robots.txt tightening landed.** Added `src/pages/404.astro` matching the site's voice: ribbon "404 · Off the map" / "No such plate", H1 "Lost.", subtitle "That URL doesn't match anything shipped…", then four navigation cards (Home / Work / Resume / Contact) each with a one-liner descriptor. Page has `noindex={true}` so search engines don't index dead URLs. Updated `public/robots.txt` to add `Disallow: /lab/` — belt-and-braces with the existing noindex meta on lab pages. Verified both: 404 returns proper 404 status with custom HTML; robots.txt returns 200 with the new directive.
- _2026-06-08_ — **Phase 4 content polish landed (4 of 5 items).** Polished `/resume` description from `"Full resume for X — experience, skills, education, contact."` to a role-aware specific sentence; same for `/contact` (was `"Get in touch with X."`). Added a status-badge legend under the stats grid on `/work` — single line explaining verified / brand-verified / unlisted / internal / early. Verified `sitemap.xml.ts` already excludes `/lab/*` by enumeration. Confirmed `CreativeWorkJsonLd.astro` already uses `MobileApplication` (a `SoftwareApplication` subtype) when there's an app-store link — the original task spec was actually less specific than the live code. **Outstanding:** `site` URL is still `https://mitulvaghasiya.com` in `astro.config.mjs` and `sitemap.xml.ts` — original audit flagged this as placeholder; user to confirm whether this is the real domain or a placeholder that needs replacement.
- _2026-06-08_ — **Phase 3 narrative polish landed (scoped down).** After re-reading all 19 case studies in full to assess where the body copy was actually thin, only 3 needed real expansion: `careshare`, `synergy`, `warden-tracker`. Each had its Overview, Problem & context, My approach, and Challenges sections extended with substantive prose — no invented facts, just unpacking the reasoning behind each existing bullet. Voice matches the foresite/lili gold-standard per 0.18. The other 16 case studies were either already substantive from Round 1+2 enrichment (most) or intentionally thin by design (radio-station). Net effect: the "9 case studies to walk" estimate was conservative; actual scope was 3.
- _2026-06-08_ — **Per-MDX TODO cleanup pass landed.** 9 of 19 case studies had resolved `todos:` entries pruned (bookmrk-delivery, bookmrk, foresite, kgk-hrms, lili, locket, lux-ecards, marline-media, studymap). Removed items were either already answered in prior decisions (foresite role focus, lili Android, studymap rebrand) or already documented in the body copy (bookmrk install path, kgk-hrms iOS framing, locket release window, marline delisting). The remaining 10 files keep genuine open items — mostly `Confirm dates.` and asset-gap requests (screenshots). No content/body edits made; frontmatter only.
- _2026-06-06_ — **Phase 2 landed and verified in browser.** Spec Sheets / Datasheet / Catalog / Vol. I-III vocabulary dropped from nav, page H1s, ribbons, footer, Base titles, and SpecimenHeading instances. Plate numbering kept on case studies per 0.1. Title tag pattern includes role on every page. Story pull-quote rewritten with derived counts (currently "Six years, nineteen apps shipped, thirteen still live in the stores"). Numbers stats rebalanced to lead with verified count and fold cities+continents into one tile. Lead.astro CTAs cleaned. Now console separator and Shipping subtitle decoupled from KGK Diamonds. Verified via fetch-based eval across `/`, `/work`, `/resume`, `/contact`, `/work/kgk-buy-diamonds` — no server errors, all page titles include the role, no visible Spec Sheets vocabulary remaining (only dev-only CSS comments). One verified-count clarification: 11 → 13 (not 14) — math: 11 original + Marline Media + BookMrk + Setera - Studymap = 13. Pull-quote auto-displays "thirteen" correctly.

---

## Coordination notes

- **Other session active** — confirm what they own before editing overlapping files.
- Suggested split: that session = components/layout/design, this session = content (MDX + profile.ts + copy).
- If the other session is doing design work, we are naturally complementary (we do copy, they do shapes).
