---
title: "Example note title"
description: "One-line summary — used in the listing, meta description, and RSS."
date: 2026-06-15
# updated: 2026-06-20
tags: ["flutter", "performance"]
draft: true
---

This file is a **template**. Astro ignores files prefixed with `_`, so it never
publishes. To write a real post: copy this to e.g. `flutter-method-channels.mdx`,
fill in the frontmatter, set `draft: false`, and write the body in Markdown/MDX.

The moment one non-draft note exists, the **Notes** nav link, the `/notes` index,
and the `/rss.xml` feed all light up automatically — no other wiring needed.

## A heading

A paragraph with a [link](https://example.com) and some `inline code`.

```dart
void main() => runApp(const MyApp());
```
