---
// Single LABEL ······ value row with a dotted leader.
// Used inside a <dl class="spec-rows">.
interface Props {
  label: string;
}
const { label } = Astro.props;
---

<div class="spec-row">
  <dt>{label}</dt>
  <span class="leader" aria-hidden="true"></span>
  <dd><slot /></dd>
</div>
