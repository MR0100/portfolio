---
import { profile } from "~/data/profile";

const year = new Date().getFullYear();

const navLinks = [
  { href: "/", label: "Home" },
  { href: "/work", label: "Work" },
  { href: "/resume", label: "Resume" },
  { href: "/contact", label: "Contact" },
];
---

<footer class="footer-bg relative z-10 mt-2 border-t border-border/60 overflow-hidden">
  <!-- Static gradient-mesh atmosphere — no animation. Accent teal glow top-left,
       sepia warm glow bottom-right, soft vignette pulls focus to centre. -->
  <div class="footer-mesh" aria-hidden="true"></div>

  <div class="container-page relative z-10 py-10 md:py-12">
    <div class="grid gap-10 md:grid-cols-[1.5fr_1fr_1fr]">

      <!-- ── Identity ── -->
      <div class="space-y-3">
        <div class="font-display text-xl font-semibold text-text tracking-tight">
          {profile.shortName}<span class="text-accent">.</span>
        </div>
        <div class="font-mono text-[10px] uppercase tracking-[0.22em] text-muted leading-relaxed">
          {profile.headline}
        </div>
        <div class="font-mono text-[10px] uppercase tracking-[0.22em] text-muted">
          {profile.location}
        </div>
        <div class="pt-3 flex items-center gap-2 font-mono text-[10px] uppercase tracking-[0.22em]">
          <span class="h-1.5 w-1.5 rounded-full bg-accent" aria-hidden="true"></span>
          <span class="text-sepia">Currently</span>
          <span class="text-text">— Mobile lead at KGK InfoTech</span>
        </div>
      </div>

      <!-- ── Sitemap ── -->
      <nav class="space-y-1.5" aria-label="Footer navigation">
        <div class="eyebrow mb-3">Sitemap</div>
        {navLinks.map((l) => (
          <a
            href={l.href}
            class="block font-mono text-[11px] uppercase tracking-[0.18em] text-muted hover:text-accent transition-colors py-1"
          >
            → {l.label}
          </a>
        ))}
      </nav>

      <!-- ── Channels ── -->
      <div class="space-y-1.5">
        <div class="eyebrow mb-3">Reach out</div>
        <a
          href={profile.socials.email}
          class="block font-mono text-[11px] uppercase tracking-[0.18em] text-muted hover:text-accent transition-colors py-1"
        >
          → Email
        </a>
        <a
          href={profile.socials.github}
          target="_blank"
          rel="noreferrer noopener"
          class="block font-mono text-[11px] uppercase tracking-[0.18em] text-muted hover:text-accent transition-colors py-1"
        >
          → GitHub
        </a>
        <a
          href={profile.socials.linkedin}
          target="_blank"
          rel="noreferrer noopener"
          class="block font-mono text-[11px] uppercase tracking-[0.18em] text-muted hover:text-accent transition-colors py-1"
        >
          → LinkedIn
        </a>
        <a
          href={profile.socials.twitter}
          target="_blank"
          rel="noreferrer noopener"
          class="block font-mono text-[11px] uppercase tracking-[0.18em] text-muted hover:text-accent transition-colors py-1"
        >
          → Twitter / X
        </a>
      </div>
    </div>

    <!-- ── Bottom strip ── -->
    <div class="mt-10 pt-5 border-t border-border/40 flex flex-wrap items-center justify-between gap-4 font-mono text-[10px] uppercase tracking-[0.24em] text-muted">
      <div>© {year} {profile.name}</div>
      <button
        type="button"
        data-footer-top
        class="inline-flex items-center gap-1.5 hover:text-accent transition-colors group"
        aria-label="Scroll to top"
      >
        <span aria-hidden="true" class="transition-transform group-hover:-translate-y-0.5">↑</span>
        Back to top
      </button>
    </div>
  </div>
</footer>

<style>
  /* Footer atmosphere — clean elevated surface tone with subtle accent glow.
     The black-overlay vignette is gone; the footer now sits on `surface`
     (slightly raised above page bg) for visual hierarchy that harmonizes
     with the rest of the site instead of feeling muddied. */
  .footer-bg {
    background-color: rgb(var(--surface));
  }
  .footer-mesh {
    position: absolute;
    inset: 0;
    z-index: 0;
    pointer-events: none;
    background:
      /* Very subtle accent corner glows — barely-there */
      radial-gradient(60% 70% at 10% 0%, rgb(var(--accent) / 0.035), transparent 55%),
      radial-gradient(55% 60% at 90% 100%, rgb(var(--accent-soft) / 0.06), transparent 55%);
  }
  /* Thin accent line at the very top edge — softer */
  .footer-bg::before {
    content: "";
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent 0%, rgb(var(--accent) / 0.22) 50%, transparent 100%);
    z-index: 1;
  }
</style>

<script is:inline>
  (function () {
    const btn = document.querySelector("[data-footer-top]");
    if (!btn) return;
    btn.addEventListener("click", () => {
      window.scrollTo({ top: 0, behavior: "smooth" });
    });
  })();
</script>
