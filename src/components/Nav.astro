---
import { profile } from "~/data/profile";
import ThemeToggle from "~/components/ThemeToggle.astro";

const links = [
  { href: "/", label: "Home" },
  { href: "/work", label: "Work" },
  { href: "/resume", label: "Resume" },
  { href: "/contact", label: "Contact" },
];

const path = Astro.url.pathname;
const isActive = (href: string) => (href === "/" ? path === "/" : path.startsWith(href));
---

<header class="sticky top-0 z-40 backdrop-blur supports-[backdrop-filter]:bg-bg/80 border-b border-border/60">
  <nav class="container-page flex h-16 items-center justify-between">
    <a href="/" class="flex items-baseline gap-2.5 group">
      <span class="font-display text-lg font-semibold tracking-tight">
        M. Vaghasiya<span class="text-accent">.</span>
      </span>
    </a>

    <div class="flex items-center gap-1 md:gap-2">
      <ul class="hidden md:flex items-center gap-0.5">
        {
          links.map(({ href, label }) => (
            <li>
              <a
                href={href}
                class:list={[
                  "px-3 py-2 rounded-md font-mono text-[11px] uppercase tracking-[0.18em] transition-colors",
                  isActive(href) ? "text-accent" : "text-muted hover:text-text",
                ]}
                aria-current={isActive(href) ? "page" : undefined}
              >
                {label}
              </a>
            </li>
          ))
        }
      </ul>

      <ThemeToggle />
    </div>
  </nav>
</header>
